#include<stdio.h>

int main()
{
	int N;
	scanf("%d", &N);
	for(int testCase= 1; testCase<= N; testCase++)
	{
        int A, B;
        scanf("%d %d", &A, &B);
       
	   int total= A-B; 
       while (A!=B) 
	   {
            int total = A-B;  
        }
        printf("Case #%d: %d\n", testCase, total);
	}
    return 0;
}
